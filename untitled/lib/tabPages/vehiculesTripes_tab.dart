import 'package:flutter/material.dart';

class VehiculesTripesTabPqge extends StatefulWidget {
  const VehiculesTripesTabPqge({Key? key}) : super(key: key);

  @override
  State<VehiculesTripesTabPqge> createState() => _VehiculesTripesTabPqgeState();
}

class _VehiculesTripesTabPqgeState extends State<VehiculesTripesTabPqge> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Tripes page ',
      ),
    );
  }
}
